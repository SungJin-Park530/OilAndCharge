# OilAndCharge — 구현 범위 정리 (MVP 우선순위)

> UI 목업(피그마) 결과 검토 후 작성. 지금 단계에서 "구현할 것"과 "보류할 것"을 구분하기 위한 문서입니다.

## 지금 우선 구현하는 핵심 로직

- [ ] **MVP 범위는 휘발유 차량으로 한정** (전기차/충전소는 이번 MVP 제외 — 아래 보류 항목 참고)
- [ ] 오피넷 API 연동 (반경 내 주유소 검색 `aroundAll.do` 호출 → 응답 파싱, `prodcd=B027` 휘발유 고정)
- [ ] 좌표 변환 (WGS84 ↔ KATEC/TM128)
- [ ] 왕복 이동비용 계산: `(왕복거리 ÷ 연비) × 리터당가격`
- [ ] 실질 소요 비용 계산: `왕복 이동비용 + (리터당가격 × 주유량)`
- [ ] DB 캐싱 (`FuelPriceCache` 테이블 — 일정 시간 내 재조회 시 API 재호출 없이 DB에서 반환)
- [ ] 차량 등록 → 콤보박스 선택 → 저장된 연비값을 계산에 반영
- [ ] 카카오맵으로 지도 표시 + 마커 렌더링

## 지금은 보류하고 메모만 해두는 항목

- [ ] 전기차/충전소 관련 기능 전체 (경유·LPG 등 휘발유 외 유종 포함) — MVP는 휘발유 차량으로 한정
- [ ] 사용자 위치 직접 지정 UI (현재는 하드코딩된 위치 전제)
- [ ] 차량 삭제/수정 기능 (현재는 등록만 가능)
- [ ] 즐겨찾기 별 아이콘의 실제 토글 로직 (현재는 시각 요소만 존재)
- [ ] 지도 확대/축소 버튼 실제 동작
- [ ] "거리순 | 가격순" 정렬 토글 실제 동작
- [ ] 지도 마커 클릭 시 카드 리스트와 연동(하이라이트)되는 상호작용
- [ ] 주유량 입력 필드의 전역 반영 로직 (입력 즉시 모든 카드에 실시간 계산 반영)

## 팀 논의가 필요한 미해결 사항

- **차량에 이미 남아있는 기존 주유 잔량의 가치 산정 방식**
  현재 아이디어: 사용자 위치 기준 지역 평균 기름값으로 기존 잔량의 가치를 산정하는 방안. 확정된 규칙은 아니며, 시스템 로직에 영향을 주는 사항이라 팀 합의가 필요함.

## 기술적으로 짚어둘 참고 사항

### 좌표계가 두 종류로 나뉜다는 점

이 프로젝트는 좌표계를 두 가지 다른 목적으로 사용합니다. 헷갈리기 쉬운 부분이라 명시해둡니다.

| 용도 | 좌표계 | 사용처 |
|---|---|---|
| 오피넷 API 호출 (검색 조건) | **KATEC (TM128)** | `aroundAll.do`의 `x`, `y` 파라미터 |
| 카카오맵 화면 표시 (마커) | **WGS84** (위도/경도) | 카카오맵 JS API — 일반적인 위도/경도를 그대로 사용 |

즉 사용자의 GPS 위치(WGS84)를 오피넷에 보낼 땐 KATEC으로 변환해야 하고, 오피넷 응답에 담긴 주유소 좌표(KATEC, `GIS_X_COOR`/`GIS_Y_COOR`)를 지도에 마커로 찍을 땐 다시 WGS84로 역변환해야 합니다.

**간단한 변환 원리**: `pyproj` 라이브러리로 두 좌표계를 각각 `Proj` 객체로 정의한 뒤, 하나의 좌표계 값을 다른 좌표계 값으로 투영(project)해주는 방식입니다. 아래는 간단한 함수 예시입니다 (최신 pyproj 기준 — `Transformer` 사용을 권장):

```python
from pyproj import Transformer

# WGS84(위도/경도) → KATEC/TM128 변환기
to_katec = Transformer.from_crs("EPSG:4326", "TM128_정의", always_xy=True)

def wgs84_to_katec(lon, lat):
    x, y = to_katec.transform(lon, lat)
    return x, y

# KATEC/TM128 → WGS84 변환기 (역방향)
to_wgs84 = Transformer.from_crs("TM128_정의", "EPSG:4326", always_xy=True)

def katec_to_wgs84(x, y):
    lon, lat = to_wgs84.transform(x, y)
    return lon, lat
```

`TM128_정의` 부분은 실제 TM128(KATEC 계열) 좌표계의 투영 정의 문자열(proj4 문자열)로 채워야 합니다. 참고 구현: https://gist.github.com/allieus/1180051/ab33229e820a5eb60f8c7971b8d1f1fc8f2cfabb (단, 이 gist는 예전 `pyproj.transform()` 방식이라 최신 버전에서는 `Transformer`로 옮겨써야 함)

**정밀도 관련**: 이 변환은 완벽한 정밀도가 목표가 아닙니다. MVP 단계에서는 수십~수백 미터 수준의 오차는 감수하고, "부산 주유소가 서울에 뜨는" 등의 극단적 오차만 발생하지 않으면 됩니다. 정밀 보정은 추후 과제로 미룹니다.

### 데이터 출처 표기 (공공누리 라이선스)

- 오피넷(한국석유공사) 데이터 활용 시 출처 표기
- README와 화면 footer에 출처 명시 필요

## 참고 자료

- 오피넷 반경 내 주유소 API 활용 개발 후기: https://velog.io/@nuguoil/2.-Datasets-%EC%A7%80%EC%97%AD%EB%B3%84-%EC%A3%BC%EC%9C%A0%EC%86%8C-%EC%9C%84%EC%B9%98-%EC%A0%95%EB%B3%B4-%EB%B0%8F-%EA%B0%80%EA%B2%A9-%EC%A0%95%EB%B3%B4-%EB%8D%B0%EC%9D%B4%ED%84%B0%EC%85%8B%EA%B3%BC-%EC%82%AC%EC%9A%A9%EC%9E%90%EC%9D%98-%ED%98%84%EC%9E%AC-%EC%9C%84%EC%B9%98-%EB%B0%9B%EA%B8%B0
- 좌표 변환 참고 gist: https://gist.github.com/allieus/1180051/ab33229e820a5eb60f8c7971b8d1f1fc8f2cfabb
